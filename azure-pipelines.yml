trigger:
- master

pr: none

pool:
  vmImage: 'ubuntu-latest'

steps:
- checkout: self
  submodules: true

- script: yarn
  displayName: 'Yarn'

- pwsh: ./publish.ps1
  displayName: 'Publish plugins'
